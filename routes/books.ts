import { Router } from 'express';


const router:Router = Router();

//GET
router.get('/', ()=>{});

//GET: id
router.get('/:id', ()=>{});

//POST
router.post('/', () =>{});

//PUT
router.put('/', () =>{});

//DELETE
router.delete('/', () =>{});

export = router;
